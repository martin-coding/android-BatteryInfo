<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BatteryInfo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bar</item>
        <item name="colorPrimaryDark">@color/bar</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="background">@color/background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background</item>
        <item name="colorOnSecondary">@color/text</item>
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/bar</item> <!-- Already in colorPrimaryDark -->
        <item name="actionOverflowButtonStyle">@style/OverflowButtonStyle</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
</resources>